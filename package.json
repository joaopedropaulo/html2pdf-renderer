{
  "name": "resume-generator",
  "version": "1.0.0",
  "description": "A Node.js application that generates a resume based on a template and input data",
  "main": "server.js",
  "scripts": {
    "start": "node src/server",
    "server": "nodemon -L src/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-validator": "^6.6.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
